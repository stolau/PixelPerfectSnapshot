{
  "name": "pixelperfectsnapshot-monorepo",
  "private": true,
  "workspaces": [
    "packages/client",
    "viewer",
    "examples/demo-app"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "test": "npm test --workspaces"
  },
  "engines": {
    "node": ">=20"
  }
}
